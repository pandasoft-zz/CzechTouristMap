# themes/

This directory stores generated Mapsforge render theme XML files.

The files here are **build output** — generated by running `./build-theme.sh` from the project root.
Do not edit them manually; edit the XSLT source templates in `src/styles/` instead.

This directory exists to give the tile-server a place to mount themes from.
